#include "main.h"
#include <stdio.h>

/**
 * main -  function prints the name of the program
 * @argc: size of argv
 * @argv: arguments
 * return: (0) if successful 
 */

int main(int argc, char *argv[]) 
{
	printf("%s\n", argv[0]);
	return (0);
}
